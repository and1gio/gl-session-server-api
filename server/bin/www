#!/usr/bin/env node

/**
 * Module dependencies.
 */
var initializersLoader = require("./initializers-loader");

/**
 * load initializers
 */
var app = {
    rootFolder: __dirname + '/../',
    env: process.env.NODE_ENV,
    moment: require('moment')
};


initializersLoader(app, function (err) {

});